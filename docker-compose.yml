services:
  monitor:
    build: .
    command: monitor.py
    volumes:
      - .:/app
    restart: unless-stopped

  telegram-bot:
    build: .
    command: telegram_bot.py
    volumes:
      - .:/app
    restart: unless-stopped
