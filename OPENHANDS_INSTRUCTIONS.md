Repository root for this project:

/projects/agents/infra_agent_v2

Before making changes verify:

pwd
git rev-parse --show-toplevel

Return the results if requested.

Do not create or use projects under:

/home/openhands/workspace/project/

Persistent repositories belong under:

/projects/agents/

When working on infra_agent_v2, use:

/projects/agents/infra_agent_v2

When creating a new agent project, create it as a sibling repository under:

/projects/agents/<agent_name>

Do not create new agent projects inside infra_agent_v2 unless explicitly instructed.

Use the current repository as the source of truth for this project.

